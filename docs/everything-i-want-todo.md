# Everything I Want TODO

Status legend: complete means implemented, documented, and covered by at least one verification path in this repo.

- [x] Define a hard truth-state system that separates hypothesis, design sketch, unverified, machine-proven, source-backed, and production-ready claims.
- [x] Enforce evidence requirements before claims move into stronger truth states.
- [x] Build a repeatable eval runner that records every run as structured evidence.
- [x] Add a reusable completion gate for this repo and future repos.
- [x] Add model-seat attach and heartbeat paths without pretending that a profile alone is control.
- [x] Add a model-oriented MCP server so model clients can run lab tools directly.
- [x] Add a human-friendly dashboard for status, evals, claims, tools, seats, and notes.
- [x] Add private model/operator tools that are useful to model seats even when they are not human-facing.
- [x] Add a tool registry that treats tools as pointers plus verification recipes, not as proof they are running.
- [x] Add session notes so finished work leaves a durable local handoff.
- [x] Keep runtime evidence separate from source review.
- [x] Add source-status and git-source-clean proof for that separation.
- [x] Add syntax, unit, HTTP, dashboard asset, public-export, secret-scan, markdown-link, and TODO-completion checks.
- [x] Add a public export lane that can clone/share only the selected public subset.
- [x] Add public/private documentation so internal work and shareable work do not get mixed.
- [x] Add a standalone Windows launcher for local use.
- [x] Add a scheduled autonomy cycle so the lab can check itself without a human prompt.
- [x] Add a failure inbox so broken gates become durable tasks.
- [x] Add a Mystro completion handoff so project completion can be blocked by Truth Lab evidence.
- [x] Add a tool promotion flow so new tools are classified as private, public-share candidate, or rejected until verified.
- [x] Add model self-eval packs for hallucination boundaries and tool-use discipline.
- [x] Add proof-bundle generation so final handbacks can point to durable machine evidence.
- [x] Add repository redundancy tracking for GitHub/GitLab backup and public-share destinations.
- [x] Add a next-work decision ledger so self-directed choices are recorded instead of being chat-only.
- [x] Add public first-run examples and model-client examples.
- [x] Add docs for the architecture, model protocol, eval checks, truth states, public sharing, and standalone app.
- [x] Add a final self-evaluation pass that asks whether anything obvious is still missing.
- [x] Capture final sparks found during the process and either implement them or explicitly mark why they are future work.

## Final Self-Evaluation

This list is the finite v1 scope I can honestly complete inside this build without pretending to have built an AI model, cloud product, or OS-level autonomous agent. The remaining ideas in `docs/sparks.md` are deliberately labeled as future expansion, not hidden unfinished promises.
