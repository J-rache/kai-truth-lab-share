# Lab Room

This folder is the local bench.

Committed:

- `lab-config.json`
- `evals/*.json`
- docs that explain the local contract

Ignored runtime:

- `runs/`
- `session-notes/`
- `runtime/`

The separation is intentional. Source review should stay readable while local evidence still exists on disk.

